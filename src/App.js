import { toHaveFormValues } from "@testing-library/jest-dom/matcher"
const valueprinter = ({name})=>
function App(value) {
  return (
    
    <div className="App">
      <p>{value} </p>
    </div>
  );
}

export default App;
